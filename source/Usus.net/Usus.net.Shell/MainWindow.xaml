<Window x:Class="andrena.Usus.net.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:andrena.Usus.net.View;assembly=Usus.net.View"
        
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Background="Green">
            <Button Content="Analyze" HorizontalAlignment="Right" Click="Button_Click" />
        </Grid>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <view:Cockpit x:Name="Cockpit" Grid.Row="0" Grid.Column="0"/>
            <view:Hotspots x:Name="Hotspots" Grid.Row="0" Grid.Column="1"/>
        </Grid>
    </Grid>
</Window>
